```log
保持状态变更合法性： 1.状态机 2.锁机制同步解决并发冲突 3.可追溯，事务变更上下文 4.异常捕获 全链路监控

原子性更新：事务回滚等
状态变更失败补偿：事务回滚，单数据库，无外部操作时用。
                saga补偿，有创建其他内部函数或方法的需求，例如创建工单等。幂等性
                外部调用补偿，一样要确保幂等性。结果一致原则，中间不一定要求一致

状态历史追溯
数据库审计，高并发压力大，中小型系统使用。
数据库溯源，将数据库变更状态转为不可变事件流
```
